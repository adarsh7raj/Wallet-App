{
  "extends": "@repo/typescript-config/react-library.json",
  "compilerOptions": {
    // Outputs compiled files in the package root, or specify a folder if preferred
  },
  "include": ["**/*.ts", "**/*.tsx"], // Includes all TypeScript files in the package
  "exclude": ["node_modules"] // Excludes the node_modules folder
}
